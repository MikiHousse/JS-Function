//№ 1
//Функция принимает на фход дерево и возвращает новое, элементами которого являются дети вложенных узлоа
const tree = [1, 2, [3, 5], [[4, 3], 2]]
const removeFirstLavel = (tre) => {
    const nodes = tre.filter(Array.isArray);
    return nodes.flat()
};
console.log(removeFirstLavel(tree))

//№ 2
//Функция создает и возвращает файловую систему как появиться подписка сделать задание !!!!!!!!!!!!!!!!!
const sistem = () => {

}

//№ 3
